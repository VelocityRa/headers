typedef struct _LIST_ENTRY
{
  struct _LIST_ENTRY* Flink;
  struct _LIST_ENTRY* Blink;
} LIST_ENTRY, *PLIST_ENTRY; 

typedef struct _HALP_ALLOC_CONTEXT
{
  struct _LIST_ENTRY BufferList;
  U64 Lock;
} HALP_ALLOC_CONTEXT, *PHALP_ALLOC_CONTEXT; 

